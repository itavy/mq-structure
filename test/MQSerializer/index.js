'use strict';

describe('MQSerializer', () => {
  require('./Initialization'); // eslint-disable-line global-require
  require('./getUnserializer'); // eslint-disable-line global-require
  require('./Serialize'); // eslint-disable-line global-require
  require('./Unserialize'); // eslint-disable-line global-require
  require('./Functionality'); // eslint-disable-line global-require
});
