'use strict';

describe('MQMessageV1', () => {
  require('./Initialization'); // eslint-disable-line global-require
  require('./ToJSON'); // eslint-disable-line global-require
  require('./SingletonSerializer'); // eslint-disable-line global-require
  require('./ToPB'); // eslint-disable-line global-require
});
